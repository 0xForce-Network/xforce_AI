# xforce_AI workspace seed

Files placed in this directory are copied into the runtime workspace only when
the destination path does not already exist. The F005 persistence boot fragment
uses this directory as the safe default workspace seed contract.
