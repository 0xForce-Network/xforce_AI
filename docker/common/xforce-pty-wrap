#!/usr/bin/env bash
set -Eeuo pipefail

export PYTHONPATH="/opt/xforce-ai/lib:${PYTHONPATH:-}"
python_bin="${VENV_DIR:-/venv/main}/bin/python"
if [ ! -x "$python_bin" ]; then
  python_bin="$(command -v python3)"
fi

exec "$python_bin" -m pty_wrapper "$@"
